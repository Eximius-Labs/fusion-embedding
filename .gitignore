.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
.env
.env.*
!.env.example

# Internal docs / research findings — kept local, not tracked
docs/

# Eval / training result JSONs — local artifacts, not tracked
results/

# Claude Code local session/config — not tracked
.claude/

# Release build artifacts + internal runbook — the shipped files live on the HF repo
release/out/
release/UPLOAD.md
fe2_release/out/
submission/
data/
_staging_space/
thermal_pack_release/
scripts/_local/

# Model release staging dirs: weights live on the HF repos, not in git
tactus_mat_release/

# LaTeX build artifacts and local render/analysis dirs
papers/**/*.aux
papers/**/*.log
papers/**/*.out
papers/**/*.pdf
!papers/**/fig_*.pdf
papers/**/prev/
papers/**/analysis/
papers/arxiv_submissions/
