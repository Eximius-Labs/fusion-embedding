"""Processor package for ``SGLANG_EXTERNAL_MM_PROCESSOR_PACKAGE``.

SGLang's ``import_processors`` imports every module in this package and registers any
``BaseMultimodalProcessor`` subclass through its ``models`` attribute.
"""
