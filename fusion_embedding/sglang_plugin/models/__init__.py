"""Model package for ``SGLANG_EXTERNAL_MODEL_PACKAGE``.

SGLang's ``ModelRegistry.register`` imports every module in this package and registers
any class assigned to ``EntryClass``, keyed by the class ``__name__``.
"""
